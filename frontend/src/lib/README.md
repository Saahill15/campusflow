# Frontend Library

This folder is for shared utilities, API clients, route helpers, and design system wrappers.

## Suggested structure

- `api/` — REST API client and service abstractions
- `hooks/` — reusable React hooks
- `types/` — shared type definitions for frontend models
- `utils/` — non-UI utilities and helpers
