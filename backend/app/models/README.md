# Models

This folder contains SQLAlchemy ORM models for platform entities.

## Purpose

- Define database schemas for future modules such as users, events, registrations, payments, and certificates
- Keep model definitions separate from business logic
