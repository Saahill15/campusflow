# API

Organize REST endpoints by version and feature area.

## Suggested layout

- `api/v1/` — versioned route groups
- `dependencies.py` — request dependencies and security wiring
- `router.py` — root API router registration
